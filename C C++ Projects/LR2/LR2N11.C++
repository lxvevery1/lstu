#include<conio.h>
#include<stdio.h>
#include<iostream>
#include<windows.h>
#include<locale.h>
#include<math.h>
using namespace std;

int main()
{
    setlocale(LC_ALL, "");
    double y = 63, x, a = 1, b = 2;
    y = 63;
    cout << "¬ведите X: ";
    cin >> x;

    for (int 63 : b)
        x - b;
        b = b * 2;

    while (a < 63)
        x - a;
        a = a * 2 + 1;
        if (a = 63)
}

