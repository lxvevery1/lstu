#include<stdio.h>
#include<math.h>

int main()
{

    double x, y, z, a ,b;

    a = ((sqrt(fabs(x - 1))) - cbrt(fabs(y)))/(1 + (pow(x,2)/2) + (pow(y, 2)/4));

    b = x * ((atan(z)) + exp(-(x+3)));

    printf("x:", a);    
    printf("y:", b); 

    return 0;


}  