#include <stdio.h>
#include <math.h>

int main()
{

    double x, y;
    printf("x = ");
    scanf("\n%lf", &x);
    printf("y = ");
    scanf("\n%lf", &y);

    if (!(x > 1 || x < -1));
    if (y >= 0 && y <= 1 && abs(x) <= y);
    (y < 0 && y >= -2);

    return 0;

}