//82. Дано действительное число X. Вычислить (x-2)(x-4)(x-8)...(x-64)/(x-1)(x-3)(x-7)...(x-63)
#include <iostream>
#include <windows.h>
#include <locale.h> 
using namespace std;

int main() 
{   
    auto a;
    setlocale(LC_ALL, "");
    const auto m = a;                     //объявление переменных
    std::cout << m;
return 0;
}

